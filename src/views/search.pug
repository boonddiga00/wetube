extends base
include mixins/video

block content
	form(method="GET")
		input(type="text", placeholder="Search by title", name="keyword")
		input(type="submit", placeholder="Search Now")
	div
		each video in videos
			+video(video)