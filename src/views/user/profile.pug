extends ../base
include ../mixins/video

block content
	img(src=`/${user.avatarUrl}`, width=100, height=100)
	h3 Created Videos
	each item in user.videos
		+video(item)
	else
		li Sorry, We can't find anything.